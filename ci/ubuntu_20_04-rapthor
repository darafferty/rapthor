ARG BASE_NAME=rapthor-base
ARG BASE_TAG=latest

RUN echo "BASE_NAME=$BASE_NAME"
RUN echo "BASE_TAG=$BASE_TAG"

FROM $BASE_NAME:$BASE_TAG

# Install Rapthor
COPY . /tmp/rapthor

RUN python3 -m pip install --no-cache-dir --upgrade /tmp/rapthor && \
    rm -rf /tmp/rapthor
